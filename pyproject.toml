[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "viqr"
version = "0.1.0"
dependencies = ["qrcode", "pillow", "opencv-python"]

[project.scripts]
viqr = "viqr:main"

[tool.setuptools]
py-modules = ["viqr"]